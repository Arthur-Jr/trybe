const changeCase = (string) => string.toUpperCase();

const getFirstLetter = (string) => string[0];

const getTogether = (string1, string2) => `${string1}${string2}`; 

module.exports = { changeCase, getFirstLetter, getTogether };
